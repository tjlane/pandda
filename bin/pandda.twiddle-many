#!/usr/bin/env pandda.python

import os, sys

files_to_twiddle = sys.argv[1:]

print '\n'.join(files_to_twiddle)


for f in files_to_twiddle:

    tag = f.split('-')[0]
    print f
    print tag

    comm = 'pandda.twiddle {!s} ../interesting_datasets/*{!s}*/pickles/dataset.pickle'.format(f,tag)

    print comm

    os.system(comm)
